Não é necessário instalar nada localmente, basta ter o docker e docker-compose instalado.
```
git clone https://github.com/wesleycremonini/react-golang
cd react-golang
docker compose up
-> http://localhost:5000
```
